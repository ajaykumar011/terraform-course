Running the command
G:\terraform-course\codepipeline-demo (master -> origin)
λ terraform init

TF creates Farget based which would cost.
TF also creates KMS which cost additonal.

Prerequisites:
For this example we need to setup Code commit first
and put some codes there which is copied from 

https://github.com/wardviaene/docker-demo

you may need to create IAM special user's credentials for codecommit account and add to repository. 
once done you may build ECR image which is used in this example for  ECS.
